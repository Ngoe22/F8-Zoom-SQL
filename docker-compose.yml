services :
  db :
    container_name: "k18_database"
    image: postgres:17
    ports :
      - "3000:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: default

    volumes:
      - './db:/var/lib/postgresql/data'